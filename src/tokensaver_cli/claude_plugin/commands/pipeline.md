---
description: Open or describe the TokenSaver Flux IA pipeline / trace view
---

Run:

```bash
tokensaver status
```

Prefer the **Latest flow** URL when printed (deep link `?tab=flows&flowId=…`). Otherwise open the Flux IA tab URL.

Explain that TokenSaver records cache, compression (CCR), PII, LLM calls, and MCP tool runs for each governed execution. Do not invent savings numbers — use what `tokensaver status` / the console shows.
